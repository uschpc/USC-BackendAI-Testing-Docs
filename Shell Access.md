# Shell access

Shell access for a compute session is available via the Console app. Click on the terminal icon and the Console app will open in a new browser tab. This is a web-based terminal app for running shell commands, which uses tmux for persistent shell sessions.
